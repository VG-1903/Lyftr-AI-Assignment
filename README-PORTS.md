# Auto Port Finder Instructions

## 🚀 How to Use:

### Windows:
1. Double-click `run.bat`
2. If ports 3000/5000 are busy, it automatically finds free ones
3. Use the URLs shown in the terminal

### Linux/Mac:
```bash
chmod +x run.sh kill-ports.sh
./run.sh